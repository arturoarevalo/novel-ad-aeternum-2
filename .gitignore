# Dependencias
node_modules/

# Salidas de build/deploy (se regeneran)
builds/*.md
builds/*.html
builds/*.epub
!builds/.gitkeep

# Estado del runner autónomo
.runner-state.json

# Informes de salud generados
# informes/ se versiona (plan §2.2): diagnóstico, scoring, changelog, informe final

# Sistema
.DS_Store
Thumbs.db

# Secretos / entorno local
.env
.env.local
deploy/.env.production
__pycache__/
*.pyc
